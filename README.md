Playing with graphql apollo client cache (add, update, remove).

Server:
From server directory

```
> npm install
> node server.js
```

Client:
From graphql-client directory

```
> npm install
> ng serve
```
